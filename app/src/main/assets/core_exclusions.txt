# Cores kept for one system and cut from another. A core's .info database field decides which
# platforms it appears under, so trimming the catalogue cannot express this: the file has to ship
# for the platform that keeps it.
#
# Each of these has its primary system elsewhere, and the platform it is cut from already keeps a
# better answer.
#
# <TAG> <core id>
SG1000 bluemsx_libretro
